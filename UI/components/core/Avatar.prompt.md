Initials/icon avatar. `advisor` = the green system presence, `human` = a caseworker/adviser, `citizen` = the user.

```jsx
<Avatar name="Aisha Khan" tone="human" />
<Avatar tone="advisor" icon={<Sparkles/>} size={28} />
```

Props: `name`, `tone` (`citizen|human|advisor`), `size`, `src`, `icon`.
