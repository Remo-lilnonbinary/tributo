Category / quick-start / filter chip. (For statuses like "Grounded" use Badge.) `interactive` makes it a selectable button, the citizen surface's quick-start prompts are interactive Tags.

```jsx
<Tag leading={<Dot/>}>Self Assessment</Tag>
<Tag interactive selected onClick={...}>Redaction demo</Tag>
```

Tones: `default | blue | live`. `interactive` + `selected` for filters/quick-starts.
