One piece of PII removed before anything left the device, the visible proof behind "your details never left here." Calm plum, mono token.

```jsx
<RedactionChip type="NAME" token="[NAME]" />
<RedactionChip type="UTR" token="••••• 8821" />
<RedactionChip type="ADDRESS" token="[ADDRESS]" count={2} />
```

Types: `NAME · ADDRESS · UTR · NINO · EMAIL · PHONE · DOB · ACCOUNT`.
